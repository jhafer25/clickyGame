import React from "react";
import "./Header.css";

const Header = props => (
    <div className="header">
        <h1>Puppy Clicker!</h1>
        <p>Remember the puppies, don't click them more than once!</p>
    </div>
);
export default Header;
